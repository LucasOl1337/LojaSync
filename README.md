# LojaSync 1.1.0

Esta raiz agora representa a versao principal do projeto.

## Estrutura

- `NOVO/lojasync/`: projeto principal ativo
- `Legacy/`: codigo e artefatos preservados do runtime anterior

## Execucao

Projeto principal:

```bat
cd NOVO\lojasync
py launcher.py
```

## Publicacao

O repositório deve refletir exatamente o estado validado localmente, com a linha principal em `NOVO/lojasync` e o restante preservado em `Legacy/`.
