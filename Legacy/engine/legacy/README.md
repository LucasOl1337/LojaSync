# Legacy

This folder contains code and artifacts moved out of the active runtime to keep the project organized for portfolio use.

## desktop_gui
- Old Tkinter desktop UI modules.
- Legacy parser manager used by the desktop flow.

## artifacts/webapp
- Historical build/dist outputs generated by PyInstaller.

These files are not required for the current web runtime (`engine/webapp/launcher.py`).
